# Combined client-reliability review: PR #1104

Exact source head `881e5111320163b6a0ac178e63aead49740feca6` vs `origin/main`
`0695da32c77bc1a56ca22c9ec383f8fd006e6a24`. Independent workstream reviews plus
a combined Grok 4.6 High source review. Prior isolated-branch ACCEPT headings
were not treated as proof.

| Review | Verdict | P1 |
| --- | --- | --- |
| Fresh: read / last-read / follow-live / pagination | ACCEPT | None |
| Fresh: notifications / NSE / verification | ACCEPT | None |
| Fresh: window / depth / CI coverage | ACCEPT | None |
| Combined Grok 4.6 High | ACCEPT | None |

No merge-blocking source defect remains on the ordinary combined paths. P2s
below are recorded, not waived. Runtime and release gates stay separate.

## What holds

- Missing last-read keeps the current viewport and Jump to Last Read. Follow-live
  may promote the provider to live-bottom without clearing that pending target.
- Pointer-down no longer drops live follow; follow is geometry
  (`live_bottom && atBottom`).
- Lagging same-stream pagination/read snapshots keep the newer view.
- Desktop Jump to Last Read adopts only when the returned snapshot contains the
  target; unused streams are discarded.
- Visible/receipt pairing still samples `latest_event_id` before items; clients
  attach a receipt token only when the painted remote tail matches.
- Replacement-edit suppression is an account override installed before pusher
  set. Notification taps keep `room_id` + `event_id` and focused context.
- NSE production graph is `nse-preview` only; `full-uniffi` stays dev. Archive
  `nm` is fail-closed. Isolation/scaffold checkers require the production
  feature invocation and the three Apple triples.
- Linux titlebar drag/maximize observation and quieter iOS room-list / desktop
  Settings depth are present as reviewed.
- Frontend CI runs `test:browser:native-timeline`. Selective UI shards include
  the new navigation and live-read cleanup selectors.

## P2 (non-blocking)

1. Poll send does not request latest; text/attachment sends do.
2. Jump to Latest can clear pending last-read before the latest open is adopted.
3. iOS Jump to Last Read closes the live stream before the focused target is
   confirmed; desktop keeps the adopted provider.
4. Desktop last-read restore still gates on unread count; iOS can restore a
   comparable in-window marker when the count is zero.
5. Empty NSE archive `find` skips `nm`; require at least one archive.
6. Isolation/scaffold checkers do not lock the fail-closed `nm` capture text.
7. Cargo tree invert still does not surface `[target.*.build-dependencies]`.
8. Invite rows remain raised; room/space settings cards are not on the Settings
   fold treatment; Linux maximize observer listens to resize, not scale.

## Proof limitations

Source-only. Combined GitHub CI, signed iOS simulator, APNs/NSE device delivery,
physical titlebar drag, and two-client live receipt proofs were not re-executed
by these reviewers. Default feature PRs into main still skip iOS simulator jobs;
the release-cut branch is the documented iOS gate.
