# P7.2 — Media download / local-delivery queue foundation

| Field | Value |
|-------|--------|
| **Task ID** | P7.2 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p7.2-media-download` |

> **Strict acceptance open.** Metadata-only `DownloadJob` queue; **no file bytes**, no SDK download.

## Summary

- `DownloadQueue` per session generation
- Kinds: Thumbnail / Original / Avatar
- Lifecycle: Queued → Fetching → Ready | Failed | Cancelled; retry + prune
- Ready stores local handle id only (cache key / path key — never bytes)
- Forbids `data:` / `javascript:` and token substrings in source / handle ids
- Marker: `matrix-media-upload-p6.4+download-p7.2` (shared media module)

## Out of scope

- SDK media download / decryption / thumbnail transform
- Embedding file bytes in IPC
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for lifecycle, forbidden schemes, retry, prune, retire
- `cargo test matrix::media` green; clippy `-D warnings`; guardrails pass
