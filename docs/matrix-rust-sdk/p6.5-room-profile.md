# P6.5 — Room profile / alias / directory / upgrade foundation

| Field | Value |
|-------|--------|
| **Task ID** | P6.5 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p6.5-room-profile` |

> **Strict acceptance open.** Pure index; no SDK room state network.

## Summary

- `RoomProfile` — name, topic, avatar URI, aliases, join/history/directory rules, upgrade pointers
- `RoomProfileIndex` — upsert/patch, alias reverse lookup, upgrade chain walk
- Caps + validation; forbids `data:` / `javascript:` avatar schemes
- Marker: `matrix-room-profile-p6.5`

## Out of scope

- Live SDK `m.room.name` / alias / directory PUT
- Avatar bytes / media download
- Production Tauri commands, dual-backend, tokens

## Acceptance (this PR)

- Unit tests for upsert, aliases, conflict, upgrade chain, forbidden schemes, retire
- `cargo test matrix::room_profile` green; clippy `-D warnings`; guardrails pass
