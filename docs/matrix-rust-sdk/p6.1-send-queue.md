# P6.1 — Outbound send queue + local-echo foundation

| Field | Value |
|-------|--------|
| **Task ID** | P6.1 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p6.1-send-queue` |

> **Strict acceptance open.** Queue + local-echo state only; no SDK `Room::send`.

## Summary

- `SendQueue` per session generation
- `OutboundTextMessage` with local txn id, room, plain-text body, `LocalEchoState`
- Lifecycle: enqueue (Sending) → Sent | Failed | Cancelled; Failed → retry → Sending
- `retire_generation` cancels in-flight sends on logout/account switch
- `prune_terminal` bounds memory after terminal states
- Marker: `matrix-send-queue-p6.1`

## Out of scope

- SDK `Room::send` / media upload / reaction send
- Production Tauri commands
- Dual-backend, tokens in logs/IPC

## Acceptance (this PR)

- Unit tests for enqueue, sent/failed/retry/cancel, validation, prune, generation retire
- `cargo test matrix::send` green; clippy `-D warnings`; guardrails pass
