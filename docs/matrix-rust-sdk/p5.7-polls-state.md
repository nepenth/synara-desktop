# P5.7 — Poll and state/membership event projection foundation

| Field | Value |
|-------|--------|
| **Task ID** | P5.7 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p5.7-polls-state` |

> **Strict acceptance open.** Pure indexes; no SDK poll send / state network.

## Summary

- `PollIndex` — MSC3381-style start / response / close; vote counts only (no voter lists)
- `StateEventIndex` — latest `(room, type, state_key)` from `TimelineStateItem`
- `MembershipEventIndex` — membership change feed from `TimelineMembershipItem`
- Caps, generation retire, privacy-safe text validation
- Marker: `matrix-polls-state-p5.7`

## Out of scope

- Live poll create/response via SDK
- Full MSC3381 unstable prefixes negotiation
- Tokens / secrets in labels or diagnostics
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for poll vote replace/close, forbidden text, state/membership upsert, retire
- `cargo test matrix::polls` green; clippy `-D warnings`; guardrails pass
