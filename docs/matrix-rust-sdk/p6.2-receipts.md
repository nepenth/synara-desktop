# P6.2 — Receipt index foundation

| Field | Value |
|-------|--------|
| **Task ID** | P6.2 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p6.2-receipts` |

> **Strict acceptance open.** Pure index over Synara `Receipt` DTOs; no SDK send.

## Summary

- `ReceiptIndex` per session generation
- Apply single/batch receipts; newer `ts` wins for same (user, type, thread)
- `latest_read` helper; list-by-room; clear room; generation retire wipes
- Marker: `matrix-receipts-p6.2`

## Out of scope

- SDK `send_single_receipt` / network
- Production Tauri commands, dual-backend, tokens in logs

## Acceptance (this PR)

- Unit tests for apply/overwrite/types/validation/retire/batch
- `cargo test matrix::receipts` green; clippy `-D warnings`; guardrails pass
