# P5.6 — Relations index foundation

| Field | Value |
|-------|--------|
| **Task ID** | P5.6 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p5.6-relations` |

> **Strict acceptance open.** Pure index over Synara `RelationRef` DTOs; no SDK send.

## Summary

- `RelationIndex` per session generation
- Annotations (reactions): apply / remove; aggregate `TimelineReactionSummaryItem` with `me`
- `m.replace`: latest wins per target event
- `m.reference` / `m.thread`: set membership + counts
- Generation retire and clear-room wipe
- Marker: `matrix-relations-p5.6`

## Out of scope

- SDK relation send / aggregation network APIs
- Production Tauri commands, dual-backend, tokens in logs
- Full thread list UI / focused timeline (P5.8)

## Acceptance (this PR)

- Unit tests for annotations/replace/reference/thread/validation/retire
- `cargo test matrix::relations` green; clippy `-D warnings`; guardrails pass
