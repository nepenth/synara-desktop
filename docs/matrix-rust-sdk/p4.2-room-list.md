# P4.2 — Room-list snapshot and delta stream (foundation)

| Field | Value |
|-------|--------|
| **Task ID** | P4.2 |
| **Date** | 2026-07-27 |
| **Base** | stacks on P4.1 (`matrix-rust/p4.1-sync-readiness`) |
| **Work branch** | `matrix-rust/p4.2-room-list` |

> **Strict acceptance / Phase 4 gate:** remain `open`. This lands the
> deterministic snapshot/delta projection for room lists (harness foundation).
> Live sliding-sync subscription / SDK Room→DTO mapping is incomplete residual work.
> No production Tauri commands. No dual-backend.

## Executive summary

- Ordered `RoomListProjection` of product [`RoomSummary`] DTOs
- Delta ops mirror VectorDiff semantics without exporting SDK types
- Monotonic `sequence` + `session_generation`; gaps force resync
- `RoomSummaryBuilder` for pure harness fixtures (no matrix-sdk imports)

## Artifact locations

| Artifact | Path |
|----------|------|
| Module | `src-tauri/src/matrix/room_list/` |
| Design | `docs/matrix-rust-sdk/p4.2-room-list.md` |

## Out of scope

Live SyncService→entries stream adapter, unread/membership product semantics
(P4.3), spaces (P4.5), production IPC publish, phase-gate close.

## Validation

- Unit: snapshot+delta reconstruct, sequence gap, stale generation, OOB ops
- Local: `cargo test --lib matrix::room_list`
