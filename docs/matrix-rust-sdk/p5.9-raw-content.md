# P5.9 — Custom Synara raw-content extraction foundation

| Field | Value |
|-------|--------|
| **Task ID** | P5.9 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p5.9-raw-content` |

> **Strict acceptance open.** Allowlisted extraction only; no full JSON dumps.

## Summary

- `RawContentExtractor` + `ExtractedContent` + `ContentValue`
- Default agent allowlist (`body`, `msgtype`, `agent_id`, …)
- Unknown fields preserved as short strings under cap (round-trip via `reassemble`)
- Forbidden keys/values (tokens, PEM, secrets)
- Marker: `matrix-raw-content-p5.9`

## Out of scope

- Full MatrixEvent graph / SDK content parsing
- Nested object graphs / media bytes
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for allowlist/unknown, forbidden, allowlist override, retire, validation
- `cargo test matrix::raw_content` green; clippy `-D warnings`; guardrails pass
