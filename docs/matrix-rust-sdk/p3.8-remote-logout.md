# P3.8 — Remote logout, local cleanup policy, recovery copy

| Field | Value |
|-------|--------|
| **Task ID** | P3.8 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p3.8-remote-logout` |

> **Strict acceptance open.** Pure remote-logout coordinator + UX copy keys.
> Local logout/wipe I/O remains P2.6; no SDK logout network yet.

## Summary

- `RemoteLogoutFlow` per session generation
- Scopes: `ThisDevice` | `AllDevices`
- Phases: Idle → RequestingRemote → LocalCleanupPending → Complete | Failed
- Local cleanup policy: `LogoutRetainStores` | `WipeAccountStore` (host calls P2.6)
- Optional skip-remote when offline (policy flag)
- `RecoveryCopyKey` catalog + `copy_for_remote_outcome` (no secrets in strings)
- Marker: `matrix-lifecycle-p2.6+session-p3.5+p3.6+remote-logout-p3.8`

## Out of scope

- Live SDK `logout` / `logout_all` network calls
- Production Tauri commands, dual-backend
- Tokens, recovery keys, or store paths in diagnostics / copy

## Acceptance (this PR)

- Unit tests for success path, skip offline, fail/retry, busy, secret-diagnostic
  reject, generation retire, copy key privacy
- `cargo test matrix::lifecycle` green; clippy `-D warnings`; guardrails pass
