# P9.1 — Widget / Element Call session registry foundation

| Field | Value |
|-------|--------|
| **Task ID** | P9.1 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p9.1-widgets` |

> **Strict acceptance open.** Pure registry over Synara `WidgetSession` DTOs; no SDK widgets.

## Summary

- `WidgetRegistry` per session generation
- begin / upsert / set_state / set_url / list / active_call_in_room
- Soft cap 32; forbids token-like markers in URLs
- Marker: `matrix-widgets-p9.1`

## Out of scope

- SDK Element Call / widget APIs
- Embedding access tokens in URLs
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for lifecycle/url guard/cap/validation/retire
- `cargo test matrix::widgets` green; clippy `-D warnings`; guardrails pass
