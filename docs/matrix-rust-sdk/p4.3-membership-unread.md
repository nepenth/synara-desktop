# P4.3 — Invites, membership, unread/highlight, marked-unread (foundation)

| Field | Value |
|-------|--------|
| **Task ID** | P4.3 |
| **Date** | 2026-07-27 |
| **Base** | stacks on P4.2 (`matrix-rust/p4.2-room-list`) |
| **Work branch** | `matrix-rust/p4.3-membership-unread` |

> **Strict acceptance / Phase 4 gate:** remain `open`. Pure projection helpers
> over `RoomSummary` fields. Live SDK unread/membership event mapping residual.

## Summary

- `RoomListScope` filters: joined / invites / unread / mentions / direct / all_active
- `RoomListBadgeCounts` tab badges (privacy-safe counters only)
- Attention total = unread-scope rooms + invites

## Artifacts

| Path | Role |
|------|------|
| `src-tauri/src/matrix/room_list/filters.rs` | Scope predicates |
| `src-tauri/src/matrix/room_list/counts.rs` | Badge aggregates |
| `docs/matrix-rust-sdk/p4.3-membership-unread.md` | This note |

## Out of scope

Live invite accept/reject mutations, push notification routing, space filters
(P4.5), production IPC, dual-backend.

## Validation

`cargo test --lib matrix::room_list`
