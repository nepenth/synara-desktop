# P3.7 — Legacy-session detection and transition coordinator

| Field | Value |
|-------|--------|
| **Task ID** | P3.7 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p3.7-legacy-transition` |

> **Strict acceptance open.** Clean-break coordinator; **never starts matrix-js-sdk**; **no token continuity**.

## Summary

- `LegacyTransition` per session generation
- Inert detection signals only (store names / markers / labels — no secrets)
- Phases: Idle → AwaitingReauth → Reauthing → EstablishingRustSession → Complete | Failed | Deferred
- Policy hard-codes: no JS client start, no token continuity, no dual-backend
- Failed transition preserves `legacy_data_retained`
- Marker: `matrix-legacy-p3.7`

## Out of scope

- Starting or constructing `matrix-js-sdk`
- Copying access/refresh tokens or device IDs into Rust crypto store
- Production Tauri commands, dual-backend selector
- Full IndexedDB cleanup UI (P3.8 / P14.2)

## Acceptance (this PR)

- Unit tests for detect/reauth/complete, fail+retry, secret-label reject, defer, retire
- `cargo test matrix::legacy` green; clippy `-D warnings`; guardrails pass
