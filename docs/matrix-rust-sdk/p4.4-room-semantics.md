# P4.4 — DM, favorite, low-priority, folder, recent-room semantics (foundation)

| Field | Value |
|-------|--------|
| **Task ID** | P4.4 |
| **Date** | 2026-07-27 |
| **Base** | stacks on P4.3 |
| **Work branch** | `matrix-rust/p4.4-room-semantics` |

> **Strict acceptance open.** Pure DTO fields + sort/filter helpers. Live
> account-data tag sync residual.

## Summary

- `RoomSummary`: `is_favorite`, `is_low_priority`, `folder_id` (serde defaults)
- Scopes: `Favorites`, `LowPriority` + `select_rooms_in_folder`
- Sorts: by name, recent activity (`last_activity_ts`), favorites-first, low-priority-last
- Product lists: Favorites (`m.favourite`) plus remaining rooms; no recents rail

## Out of scope

Live m.tag account-data apply, space hierarchy (P4.5), production IPC, dual-backend.
