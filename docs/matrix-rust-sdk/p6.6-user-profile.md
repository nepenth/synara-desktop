# P6.6 — User profile / ignore list foundation

| Field | Value |
|-------|--------|
| **Task ID** | P6.6 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p6.6-user-profile` |

> **Strict acceptance open.** Pure profile + ignore projection; **no avatar bytes**.

## Summary

- `UserProfileIndex` — own profile, peer cache (cap 512), ignore list (cap 1024)
- Avatar URI/handle only; forbids `data:` / `javascript:`
- Generation retire wipes all state
- Marker: `matrix-user-profile-p6.6`

## Out of scope

- SDK `set_display_name` / avatar upload
- Power-level edits (separate), device list (P8.2)
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for own/peer, ignore, caps, scheme forbid, retire
- `cargo test matrix::user_profile` green; clippy `-D warnings`; guardrails pass
