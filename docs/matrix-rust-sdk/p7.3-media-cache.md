# P7.3 — Media cache / retention index foundation

| Field | Value |
|-------|--------|
| **Task ID** | P7.3 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p7.3-media-cache` |

> **Strict acceptance open.** Pure metadata index; no disk I/O, no file bytes.

## Summary

- `MediaCacheIndex` — handle id, optional mxc, size_bytes, last_access_ts
- LRU `plan_eviction` / `apply_eviction`; `purge_older_than` privacy cleanup
- Caps: 4096 entries, 2 GiB logical total; forbids data:/tokens in ids
- Marker: `matrix-media-cache-p7.3`

## Out of scope

- Actual filesystem delete / SQLite media store
- SDK media download
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for upsert, LRU eviction, purge, forbidden schemes, retire
- `cargo test matrix::media_cache` green; clippy `-D warnings`; guardrails pass
