# P6.4 — Media upload queue foundation

| Field | Value |
|-------|--------|
| **Task ID** | P6.4 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p6.4-media-upload` |

> **Strict acceptance open.** Metadata-only `UploadJob` queue; **no file bytes**, no SDK upload.

## Summary

- `UploadQueue` per session generation
- Lifecycle: Queued → Uploading → Completed | Failed | Cancelled
- Progress in \[0,1\]; complete stores media handle id only
- Active cap 16; soft 100 MiB size guard; generation retire cancels active
- Marker: `matrix-media-upload-p6.4`

## Out of scope

- SDK `Media::upload` / network / encryption of media
- Embedding file bytes in IPC
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for lifecycle, validation, cap, prune, retire
- `cargo test matrix::media` green; clippy `-D warnings`; guardrails pass
