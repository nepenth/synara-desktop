# P4.6 — Room member / power-level index foundation

| Field | Value |
|-------|--------|
| **Task ID** | P4.6 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p4.6-members` |

> **Strict acceptance open.** Pure index over Synara `RoomMember` DTOs; no SDK member APIs.

## Summary

- `MemberIndex` per session generation
- Upsert / batch / get / list-by-room (power then user_id) / joined filter / highest_power
- Soft cap 4096 members/room; clear-room; generation retire wipes
- Marker: `matrix-members-p4.6`

## Out of scope

- SDK member list / power-level network APIs
- Production Tauri commands, dual-backend, tokens in logs
- Profile avatar media bytes (handles only)

## Acceptance (this PR)

- Unit tests for upsert/order/filter/cap/validation/retire
- `cargo test matrix::members` green; clippy `-D warnings`; guardrails pass
