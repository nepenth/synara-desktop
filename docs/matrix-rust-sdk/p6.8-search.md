# P6.8 — Search session / result index foundation

| Field | Value |
|-------|--------|
| **Task ID** | P6.8 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p6.8-search` |

> **Strict acceptance open.** Pure session over Synara `SearchResult` DTOs; no SDK search.

## Summary

- `SearchSession` per session generation with request ids
- begin / apply_page (append + dedup) / fail / cancel / clear
- Stale request ids ignored; cancel blocks later apply
- Soft cap 500 hits; `to_result` for IPC snapshot
- Marker: `matrix-search-p6.8`

## Out of scope

- SDK search network APIs
- Production Tauri commands, dual-backend
- Ciphertext snippets (host must privacy-filter)

## Acceptance (this PR)

- Unit tests for begin/apply/stale/cancel/dedup/validation/retire
- `cargo test matrix::search` green; clippy `-D warnings`; guardrails pass
