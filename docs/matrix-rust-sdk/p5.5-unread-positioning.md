# P5.5 — Read markers / unread positioning foundation

| Field | Value |
|-------|--------|
| **Task ID** | P5.5 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p5.5-unread-positioning` |

> **Strict acceptance open.** Pure open-position policy; no SDK receipt send.

## Summary

- `RoomReadState` — fully_read / public+private receipt / live bottom / counts
- `UnreadPositionStore` — upsert, apply `Receipt` DTOs, notification counts
- `OpenPositionPolicy` — LiveBottom | UnreadContext | ExplicitEvent | RestoreLocalViewport
- `should_show_jump_to_unread` affordance helper
- Aligns with `docs/timeline-room-state-reliability-contract.md`
- Marker: `matrix-unread-positioning-p5.5`
- Typing remains P6.3 (`matrix::typing`)

## Out of scope

- SDK send_receipt / account-data network
- Pixel viewport persistence implementation
- Production Tauri commands, dual-backend, tokens

## Acceptance (this PR)

- Unit tests for unread context, explicit route, viewport restore, receipts, jump, retire
- `cargo test matrix::unread` green; clippy `-D warnings`; guardrails pass
