# V-ROOMS.MEMBERS-READ — native member / power-level / tag read residual (tip-accurate)

| Field                                | Value                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status                               | **Honest tip-accurate residual after #395/#405/#450/#514/#516/#519** — native member enumeration, power/creator/tag read owners are live and fail-closed; direct readers are native-projection routed; remaining residual is single-member membership (`useMembership`), the retained non-native/web JS branch in `useRoomMembers`, and shared member-row/media importers that still hold a `matrix-js-sdk` import at tip |
| Tip SHA                              | `31502d53` (`origin/feature/matrix-rust-sdk-full-replacement`, 2026-08-03, `docs(matrix): final pause handoff at tip 57ab9e64 (114 importers) (#549)`)                                                                                                                                                                                                                                                                    |
| Product baseline                     | `57ab9e64` (#546, 124→114 importers); `31502d53` is a docs-only pause-handoff commit on top                                                                                                                                                                                                                                                                                                                               |
| Base                                 | `feature/matrix-rust-sdk-full-replacement` only; never `main` / umbrella #39                                                                                                                                                                                                                                                                                                                                              |
| Production `matrix-js-sdk` importers | **114** (tip inventory; plan baseline 220; ~48.2% removed)                                                                                                                                                                                                                                                                                                                                                                |
| Allowlist `pathCount`                | **114** (= `paths[]` length) — p1.6-js-sdk-import-allowlist.json at tip                                                                                                                                                                                                                                                                                                                                                   |
| Policy                               | [full-vertical-policy.md](full-vertical-policy.md) — native owner + live SDK + fail-closed; `dual_backend=false`; V-BURN **HOLD**                                                                                                                                                                                                                                                                                         |
| PR refs                              | **#395** enumeration owner · **#405** drawer/mention wiring · **#450** power/creator reads · **#514** direct power/create reader routing · **#516** native power-level-tag reads · **#519** via-servers closure                                                                                                                                                                                                           |
| Related                              | [SCOREBOARD.md](SCOREBOARD.md) (Done: Members/power partial → residual-empty slices row), [v-rooms-power-levels-read-residual.md](v-rooms-power-levels-read-residual.md), [p4.6-members.md](p4.6-members.md)                                                                                                                                                                                                              |

> **Scope guard (repeat).** This is a **docs-only** slice. It edits no product
> code, no TS, no React UI, and no inventory generator output. No
> `src-tauri/src/matrix/**` command module, no IPC registration, and no allowlist
> / `desktop-sdk-usage.{md,json}` mutation. It only re-opens this residual map at
> the true tip so the ledger no longer claims stale SHAs or unearned closures.
> The residual rows below are the ones an orchestrator can act on when product
> work resumes. This doc does **not** touch `PROGRESS.md` or `SCOREBOARD.md`.
>
> **Tip-honesty note (ADR-style, one).** The previous head of this file was
> refreshed at `fd0dfbf4`. That SHA is **not** the integration tip: `57ab9e64`
> and `31502d53` superseded it. This rewrite measures the ledger from
> `origin/feature/matrix-rust-sdk-full-replacement` and from the committed
> `desktop-sdk-usage.json` at that tip, never from a stale worktree or a copy of
> an older inventory. Claims of closure are backed by merged-PR SHAs and the
> current file contents (no `matrix-js-sdk` import where "closed" is claimed for
> a native-owner boundary, or an explicit retained non-native branch where
> "boundary" is claimed). Inventory figures are quoted verbatim from the tip
> JSON; this doc reduces no counts.

---

## 1. What is genuinely closed at tip `31502d53`

All of the following are backed by **merged PRs in the tip history** and by the
**current file contents at tip**. "Closed" here means the native desktop happy
path no longer relies on a `matrix-js-sdk` live read for that capability and the
file named as evidence no longer imports the JS SDK (or routes through a native
owner on the native session).

| #   | Capability                                                                                      | Native command (Rust)                                                                | TS native owner / evidence                                                                                                                                                                                                                                                                         | PR                               |
| --- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| 1   | Member-list enumeration (settings, people drawer, mention autocomplete, room-notes, typing row) | `matrix_room_members_snapshot` in `src-tauri/src/matrix/members/product_commands.rs` | `synara/src/app/hooks/nativeRoomMembersOwner.ts` (no `matrix-js-sdk` import; validates DTO, terminal on failure); wired by `Members.tsx`, `MembersDrawer.tsx`, `UserMentionAutocomplete.tsx`, `RoomNotesPanel.tsx`, `RoomViewTyping.tsx` via the 3-arg `useRoomMembers(mx, roomId, nativeSession)` | #395, #405                       |
| 2   | Power-level read (single + multi-room)                                                          | `matrix_room_power_levels_snapshot`                                                  | `nativeRoomPowerLevelsOwner.ts` (no import) → `usePowerLevels` / `useRoomsPowerLevels` native branch, fail-closed                                                                                                                                                                                  | #450                             |
| 3   | Creator read (single + multi-room)                                                              | `matrix_room_creators_snapshot`                                                      | `nativeRoomCreatorsOwner.ts` (no import) → `useRoomCreators` / `useRoomsCreators` native branch, fail-closed                                                                                                                                                                                       | #450                             |
| 4   | Power-level-tag READ (custom `in.synara.room.power_level_tags` metadata)                        | `matrix_room_power_level_tags_snapshot` in `members/product_commands.rs`             | `nativeRoomPowerLevelTagsOwner.ts` (no import) → `usePowerLevelTags` native branch reads persisted names/colors/icons; JS `useStateEvent` read is disabled on native                                                                                                                               | #516                             |
| 5   | Direct power/create readers (via-server selection, perfect-parent navigation)                   | — (read via native room-state projection cache)                                      | `synara/src/app/features/matrix-dto/nativeRoomStateProjection.ts` (#514), `utils/room.ts` `getAllVersionsRoomCreator` / `guessPerfectParent` take the native branch on `isNativeMatrixSession()`; `plugins/via-servers.ts` consumes `getNativeHighestPowerUserId` + native member reader           | #514                             |
| 6   | via-servers plugin closure                                                                      | —                                                                                    | `synara/src/app/plugins/via-servers.ts` **no longer imports `matrix-js-sdk`** at tip; legacy `getStateEvent` path only in the explicit non-native branch; `plugins/__tests__/via-servers.test.ts` added                                                                                            | #514 + #519                      |
| 7   | Member filter / sort / power compare / room-permission gates                                    | —                                                                                    | `useMemberFilter.ts`, `useMemberSort.ts`, `useMemberPowerCompare.ts`, `useRoomPermissions.ts` have **no `matrix-js-sdk` import** at tip (SDK-neutral DTO consumers)                                                                                                                                | closed via #405/#450 aggregation |

**Evidence files:** `hooks/__tests__/nativeRoomMembersOwner.test.ts`,
`hooks/__tests__/roomMembersWiring.test.ts`,
`hooks/__tests__/nativeRoomPowerReadOwner.test.ts` (covers power levels /
creators / tags + #519 via-server adjustments), `plugins/__tests__/via-servers.test.ts`.

---

## 2. Residual table — V-ROOMS.MEMBERS-READ at tip `31502d53`

Rows are exactly the **production importers under `synara/src`** that still
import `matrix-js-sdk` at tip and belong to the members/power/tag read family,
enumerated from the committed tip inventory JSON
(`desktop-sdk-usage.json`, `role=production`, `desktopRuntime=true`, import of
`matrix-js-sdk`). Task regex enumeration
(`member|via-servers|MembersDrawer|room/message|useGetRoom`, production role)
yields **11 files**; the members-read-relevant subset is expanded below with the
adjacent power/tag/creator helpers (`usePowerLevels`, `usePowerLevelTags`,
`useRoomCreators`, `utils/room.ts`, `utils/matrix.ts`, `Lobby.tsx`,
`UserMentionAutocomplete.tsx`) that the previous residual explicitly tracked.

### DONE / CLOSED (native owner live; no native fallback)

| Path                                                                                                                         | Role at tip                                       | Evidence                                                                                         | ID                               |
| ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------------------------------- |
| `src-tauri/src/matrix/members/product_commands.rs` (`matrix_room_members_snapshot`, `matrix_room_power_level_tags_snapshot`) | Native live-SDK producer for members + tags       | members snapshot since #395, power_level_tags_snapshot since #516                                | **Closed (native command live)** |
| `synara/src/app/hooks/nativeRoomMembersOwner.ts`                                                                             | Fail-closed native member owner                   | No `matrix-js-sdk` import; `nativeRoomMembersOwner.test.ts`                                      | **Closed**                       |
| `synara/src/app/hooks/nativeRoomPowerLevelsOwner.ts`                                                                         | Fail-closed native power owner                    | No import; `nativeRoomPowerReadOwner.test.ts`                                                    | **Closed**                       |
| `synara/src/app/hooks/nativeRoomCreatorsOwner.ts`                                                                            | Fail-closed native creator owner                  | No import; `nativeRoomPowerReadOwner.test.ts`                                                    | **Closed**                       |
| `synara/src/app/hooks/nativeRoomPowerLevelTagsOwner.ts`                                                                      | Fail-closed native tag owner                      | No import; added #516; `nativeRoomPowerReadOwner.test.ts`                                        | **Closed**                       |
| `synara/src/app/features/common-settings/members/Members.tsx`                                                                | Native member list page; no import at tip         | Not a production importer at tip                                                                 | **Closed**                       |
| `synara/src/app/features/room/Room.tsx`                                                                                      | No legacy member-list ownership; no import at tip | Not a production importer at tip (#405)                                                          | **Closed**                       |
| `synara/src/app/hooks/useMemberFilter.ts`                                                                                    | SDK-neutral filter                                | No import at tip                                                                                 | **Closed**                       |
| `synara/src/app/hooks/useMemberSort.ts`                                                                                      | SDK-neutral sort                                  | No import at tip                                                                                 | **Closed**                       |
| `synara/src/app/hooks/useMemberPowerCompare.ts`                                                                              | Native power compare                              | No import at tip                                                                                 | **Closed**                       |
| `synara/src/app/hooks/useRoomPermissions.ts`                                                                                 | Native permission gate                            | No import at tip                                                                                 | **Closed**                       |
| `synara/src/app/plugins/via-servers.ts`                                                                                      | via-server selection                              | No `matrix-js-sdk` import at tip; native member + native power projection; `via-servers.test.ts` | **Closed (#519)**                |

### OPEN — files still holding a `matrix-js-sdk` import at tip

| Path                                                                        | Residual capability and exact role                                                                                                                                                                                                                                                                                                                                                                                                          | Native-command / owner gap at tip                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `synara/src/app/hooks/useRoomMembers.ts`                                    | **Retained JS enumeration branch + legacy 2-arg overload.** Imports `MatrixClient, MatrixEvent, RoomMember, RoomMemberEvent`; when `nativeSession` is selected the hook reads `readRoomMembersWithNativeOwner` and never falls back to JS; the JS `mx.getRoom(roomId).getMembers()` / `loadMembersIfNeeded()` + `RoomMemberEvent.*` listeners path exists **only for the explicit non-native/web route and the legacy 2-argument overload** | No native gap on desktop; **OPEN only as the retained non-native/web branch + two-arg overload**. Close when non-native/web is dropped (product decision)                                                                                                                                             |
| `synara/src/app/hooks/useMembership.ts`                                     | **Single-member membership READ (`room.getMember(userId)` + `RoomMemberEvent.Membership` listener).** Imports `Room, RoomMemberEvent, RoomMemberEventHandlerMap`; consumed by `UserRoomProfile.tsx`                                                                                                                                                                                                                                         | **No native command owner for a per-user membership query** (only the bulk `matrix_room_members_snapshot`). Gap: a native `matrix_room_member_snapshot { roomId, userId }` (or reusable single-user membership projection) + fail-closed owner if this is product-parity for the DM-peer/profile read |
| `synara/src/app/features/room/MembersDrawer.tsx`                            | Members drawer renders from the **native member snapshot** (#405) but the file still imports `MatrixClient, Room` (used for media/avatar thumbnails and non-native joined-member count branch)                                                                                                                                                                                                                                              | No member-read capability gap on native desktop; **importer remains** for media + non-native count — burn-item only                                                                                                                                                                                   |
| `synara/src/app/components/member-tile/MemberTile.tsx`                      | Shared member-row renderer; imports `MatrixClient, Room, RoomMember`; accepts the shared JS/native member-list type and renders display name/avatar                                                                                                                                                                                                                                                                                         | No read gap (renders from the shared type); **importer remains** — shared UI, SDK-typed; burn-item only unless the shared type becomes fully SDK-neutral                                                                                                                                              |
| `synara/src/app/hooks/useMemberPowerTag.ts`                                 | Power-tag calculation consumes **native** tags (`usePowerLevelTags`), native creators (`useRoomCreators`), native power (`readPowerLevel`); imports `MatrixClient, Room, RoomMember` for `getPowerTagIconSrc` media resolution and the shared member type                                                                                                                                                                                   | No capability gap on native; **importer remains** for tag-icon media URL + types                                                                                                                                                                                                                      |
| `synara/src/app/hooks/usePowerLevels.ts`                                    | Native power read (#450) is live and fail-closed on native; file still imports `MatrixEvent, Room` and keeps `getStateEvent`-based legacy read for the **non-native/web** route and write-side `applyPermissionPower` helpers (PowersEditor bulk WRITE is a separate #439 slice)                                                                                                                                                            | No native read gap; **importer remains** (legacy branch + types)                                                                                                                                                                                                                                      |
| `synara/src/app/hooks/usePowerLevelTags.ts`                                 | Native tag read (#516) is live; file still imports `Room`                                                                                                                                                                                                                                                                                                                                                                                   | No read gap; **importer remains** (type + legacy `useStateEvent` for non-native)                                                                                                                                                                                                                      |
| `synara/src/app/hooks/useRoomCreators.ts`                                   | Native creator read (#450) is live; file still imports `MatrixClient, MatrixEvent, Room`                                                                                                                                                                                                                                                                                                                                                    | No read gap; **importer remains** (legacy branch + types)                                                                                                                                                                                                                                             |
| `synara/src/app/utils/room.ts`                                              | Shared room utility — direct readers (`getAllVersionsRoomCreator`, `guessPerfectParent`) are **native-routed** (#514); file still imports many JS SDK types/symbols and hosts shared member display/avatar helpers (`getMemberDisplayName`, `getMemberAvatarMxc`, `getMemberSearchStr`) used by member rows                                                                                                                                 | No native member-read gap for the routed helpers; **importer remains** as shared utility + holdover symbols (also serves unrelated timeline/message helpers — shared burn item)                                                                                                                       |
| `synara/src/app/utils/matrix.ts`                                            | DM-peer member helpers (`getOldestMember`, `guessDmRoomUserId`, `getDMRoomFor`) read live client members; plus media/upload helpers; imports `MatrixClient, MatrixError, MatrixEvent, Room, RoomMember, UploadProgress, UploadResponse`                                                                                                                                                                                                     | **DM-peer member read has no native owner** — this is the SCOREBOARD "DM-peer may remain" item; needs a native DM/DM-peer reader or explicit non-goal scope                                                                                                                                           |
| `synara/src/app/features/lobby/Lobby.tsx`                                   | Space hierarchy; imports `JoinRule, Room`                                                                                                                                                                                                                                                                                                                                                                                                   | No member-list ownership (moved to #405 drawer); **importer remains** for space/join-rule reads — outside this read residual (rooms/space join-rule vertical)                                                                                                                                         |
| `synara/src/app/components/editor/autocomplete/UserMentionAutocomplete.tsx` | Mention autocomplete selects the **native member snapshot** (#405); imports `MatrixClient, Room`                                                                                                                                                                                                                                                                                                                                            | No read gap; **importer remains** (types/media)                                                                                                                                                                                                                                                       |

### Other imports matched by the task family regex but **out of this residual's scope**

These are production importers at tip whose path matches
`member|via-servers|MembersDrawer|room/message|useGetRoom` but belong to other
verticals, listed so the enumeration is complete and defensible:

| Path                                                     | Belongs to                                   | Import                                                         |
| -------------------------------------------------------- | -------------------------------------------- | -------------------------------------------------------------- |
| `synara/src/app/features/call-status/MemberGlance.tsx`   | Call-status vertical                         | `CallMembership`, `Room`                                       |
| `synara/src/app/features/call-status/MemberSpeaking.tsx` | Call-status vertical                         | `Room`                                                         |
| `synara/src/app/features/call/CallMemberCard.tsx`        | Call vertical                                | `CallMembership`                                               |
| `synara/src/app/features/room/message/Message.tsx`       | Send/timeline vertical                       | `MatrixEvent`, `Room`, `Relations`                             |
| `synara/src/app/features/room/message/MessageEditor.tsx` | Send/timeline vertical                       | `IContent`, `IMentions`, `MatrixEvent`, `RelationType`, `Room` |
| `synara/src/app/hooks/useGetRoom.ts`                     | General room lookup helper (not member-read) | `Room`                                                         |

---

## 3. Summary for the orchestrator

- **Enumeration / power / creator / tag read: closed** at tip (`#395/#405/#450/#516`), native owners fail-closed with no JS fallback on native desktop.
- **Direct readers (via-servers, perfect-parent, creator routing): closed** (`#514` + `#519`); `via-servers.ts` no longer imports `matrix-js-sdk`.
- **Still OPEN (concrete native-command gap):**
  1. `useMembership.ts` single-user membership read → needs a native per-user membership query/owner.
  2. `utils/matrix.ts` DM-peer member helpers (`getOldestMember` / `guessDmRoomUserId` / `getDMRoomFor`) → DM-peer read is not native-owned (matches SCOREBOARD "DM-peer may remain").
  3. `useRoomMembers.ts` legacy non-native/web branch + 2-arg overload → retained product branch; close only with a web/non-native drop decision.
- **Still OPEN (importer burn items, no capability gap):** `MembersDrawer.tsx`, `MemberTile.tsx`, `useMemberPowerTag.ts`, `usePowerLevels.ts`, `usePowerLevelTags.ts`, `useRoomCreators.ts`, `useGetRoom.ts`, `Lobby.tsx`, `UserMentionAutocomplete.tsx`, `utils/room.ts`, `utils/matrix.ts` — all still hold a `matrix-js-sdk` import at tip and are part of the 114-importer inventory. None of them is a native member-read fallback; they are type/media/shared-UI or retained non-native branches that the final burn-down must drain.
- **Inventory honesty:** 114 production importers at tip (`desktop-sdk-usage.json`); this docs-only slice changes none of them. No allowlist / inventory regeneration was run or needed.

> **Scope guard (repeat).** No product code changed. If a native per-user
> membership command or DM-peer owner is to be built, that is a future product
> slice on the feature branch, not this docs slice. V-BURN stays **HOLD** until
> zero production importers and the drop-npm criteria are met.
