# P3.3 — SSO / OAuth callback lifecycle foundation

| Field | Value |
|-------|--------|
| **Task ID** | P3.3 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p3.3-sso-callback` |

> **Strict acceptance open.** Pure callback coordinator; **never stores tokens/codes**.

## Summary

- `SsoCallbackFlow` — Idle → AwaitingBrowser → CallbackReceived → Exchanging → Succeeded | Cancelled | Failed
- CSRF `state_id` match required; stale `op_id` rejected
- Redirect allowlist: `https://`, `synara:`, `synara-desktop:` — forbid `http`, `javascript`, `data`, `file`, embedded secrets
- Marker: `matrix-auth-sso-callback-p3.3`

## Out of scope

- Opening system browser / ASWebAuthenticationSession
- SDK OAuth/SSO network exchange
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for happy path, state mismatch, redirect policy, cancel/stale, retire
- `cargo test matrix::auth` green; clippy `-D warnings`; guardrails pass
- `never_stores_tokens()` invariant
