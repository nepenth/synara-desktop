# P8.2 — Device list / trust projection foundation

| Field | Value |
|-------|--------|
| **Task ID** | P8.2 |
| **Date** | 2026-07-27 |
| **Work branch** | `matrix-rust/p8.2-devices` |

> **Strict acceptance open.** Pure device summaries; **no keys / tokens / recovery material**.

## Summary

- `DeviceIndex` + `DeviceSummary` per session generation
- Upsert / list_active (own → verified → id) / set_verified / mark_deleted
- Soft cap 256 active devices; generation retire wipes
- Marker: `matrix-devices-p8.2`

## Out of scope

- SDK device keys, deletion network API
- Recovery keys, access tokens in DTOs/logs
- Production Tauri commands, dual-backend

## Acceptance (this PR)

- Unit tests for upsert/order/verify/delete/cap/validation/retire
- `cargo test matrix::devices` green; clippy `-D warnings`; guardrails pass
