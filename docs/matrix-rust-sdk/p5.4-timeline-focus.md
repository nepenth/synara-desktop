# P5.4 — Event-focused / context opening foundation

| Field | Value |
|-------|--------|
| **Task ID** | P5.4 |
| **Date** | 2026-07-28 |
| **Work branch** | `matrix-rust/p5.4-timeline-focus` |

> **Strict acceptance open.** Pure focus/navigation controller; no SDK
> `TimelineFocus` attach or context network fetch yet.

## Summary

- `TimelineFocus` per stream key + session generation
- Modes: `Live` | `Unread { marker_event_id }` | `Focused { event_id }`
- Phases: `Idle` → `LoadingContext` / `RebindingLive` → `SettlingLayout` →
  `BottomConfirmed` | `Error`
- `FocusOpenRequest` + `ContextWindow` (before/after ≤ 100); event ids `$…` only
- Outcomes report counts + `target_found` + `at_live_bottom` (items via P5.2)
- Generation retire cancels in-flight opens with privacy-safe diagnostic
- Marker: `matrix-timeline-registry-p5.1+diffs-p5.2+pagination-p5.3+focus-p5.4`

Aligns with `docs/timeline-room-state-reliability-contract.md` mode/phase model.

## Out of scope

- SDK Timeline focus attach or real `/context` fetch
- Viewport pixel restore / scroll policies
- Production Tauri commands, dual-backend, tokens or event bodies in logs

## Acceptance (this PR)

- Unit tests for focused/unread/live open, jump-latest, target-not-found,
  busy reject, validation, fail/retry, generation retire
- `cargo test matrix::timeline` green; clippy `-D warnings`; guardrails pass
- Stacks on P5.1 registry + P5.2 diffs + P5.3 pagination
