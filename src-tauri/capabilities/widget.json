{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "widget",
  "description": "Isolated experimental widget webview. No matrix_* commands, secret-store, or file-drop. The only IPC is the Rust-owned widget_bridge_post pump.",
  "windows": ["widget-*"],
  "remote": {
    "urls": [
      "https://*/*",
      "http://127.0.0.1:*/*",
      "http://localhost:*/*"
    ]
  },
  "permissions": [
    "core:default",
    "allow-widget-bridge-post"
  ]
}
