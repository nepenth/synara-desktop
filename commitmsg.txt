feat(cut): remove V-CALL — remove embedded call plugin + matrix-widget-api for a fully zero-Matrix-JS tree

Operator-authorized deep V-CALL cut (option B).

Removed:
- plugins/call, features/call, features/call-status UI + hooks/atoms (CallEmbedProvider,
  useCallEmbed/useCall/useCallSpeakers, callEmbed/callPreferences/callChat atoms, pages/CallStatusRenderer)
- matrix-widget-api from dependencies + lockfile
- native matrix/widgets module (registry, product_commands, error) + dto/widget.rs + WidgetId DTO
- call-widget-media live Synapse proof + its CI job + aggregate wiring (native proof family now 6)
- modernisation test registrations for deleted call tests

Preserved (general media, not call):
- matrix_media_config / matrix_media_download native owners relocated into the media module
  (matrix_call_media_config renamed -> matrix_media_config; capability ACL renamed accordingly)

Strengthened:
- check-v-burn-complete.mjs asserts matrix-widget-api absent from deps/devDeps/lock + no import
  coupling (20 checks, fully zero-Matrix-JS). Docs record the removal while keeping V-BURN markers.
